# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(gps_ubloxTaskLib)
ADD_LIBRARY(${GPS_UBLOX_TASKLIB_NAME} SHARED
    ${GPS_UBLOX_TASKLIB_SOURCES})
add_dependencies(${GPS_UBLOX_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${GPS_UBLOX_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${GPS_UBLOX_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GPS_UBLOX_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GPS_UBLOX_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${GPS_UBLOX_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${GPS_UBLOX_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GPS_UBLOX_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GPS_UBLOX_TASKLIB_HEADERS}
    DESTINATION include/orocos/gps_ublox)

